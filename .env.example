# URL base da API Nest (sem o sufixo /api)
NEXT_PUBLIC_VEMOVEL_API_URL=http://localhost:3001

# Quando rodar no servidor (ex.: Render) use o host interno para evitar roteamento externo
# API_URL_INTERNAL=http://localhost:3001
